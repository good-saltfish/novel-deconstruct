# 本地 pre-commit（需要网络安装 pre-commit 后执行 `pre-commit install`）
# ruff 钩子在本机网络恢复后启用；其余内置钩子零依赖。
repos:
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v5.0.0
    hooks:
      - id: trailing-whitespace
      - id: end-of-file-fixer
      - id: check-yaml
      - id: check-toml
      - id: check-added-large-files
        args: ["--maxkb=1024"]  # 版权正文保护：单文件超过 1MB 直接拦下
      - id: check-merge-conflict
  # 网络恢复后取消注释：
  # - repo: https://github.com/astral-sh/ruff-pre-commit
  #   rev: v0.6.9
  #   hooks:
  #     - id: ruff
  #       args: ["--fix"]
